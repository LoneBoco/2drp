syntax = "proto3";

package tdrp.packet;

message ClassAdd {
	message Attribute {
		uint32 id = 1;
		string name = 2;
		bool replicated = 3;
		uint32 update_rate = 4;
		oneof value {
			int64 as_int = 5;
			double as_double = 6;
			string as_string = 7;
		}
	}

	string name = 1;
	string scriptclient = 2;
	string scriptserver = 3;
	repeated Attribute attributes = 4;
}
